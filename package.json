{
  "name": "sabatini-gitlab-draft-watcher",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check src/check-once.js && node --check src/diff.js && node --check src/scrape.js && node --check src/state.js",
    "check-once": "node src/check-once.js"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "diff": "^5.2.0",
    "playwright": "1.54.1"
  },
  "engines": {
    "node": ">=20"
  }
}
